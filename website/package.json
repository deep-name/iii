{
  "name": "iii-website",
  "private": true,
  "license": "Apache-2.0",
  "version": "0.1.0",
  "description": "iii.dev — minimal static HTML site",
  "scripts": {
    "dev": "pnpm build:ai && browser-sync start --server --port 3000 --no-notify --no-open --files '*.html, *.css, fonts/*'",
    "build:ai": "node scripts/generate-machine-markdown.mjs",
    "build": "pnpm build:ai",
    "preview": "pnpm build:ai && browser-sync start --server --port 3000 --no-notify --no-open"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
